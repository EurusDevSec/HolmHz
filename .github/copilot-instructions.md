"Always prioritize using GitNexus MCP tools (search, graph query, navigation) to gather codebase context. Avoid reading full files with @ unless specifically requested, to minimize token usage and stay within quota."

"Always read #plan.md, #context.md"
